$Id: UPGRADE,v 1.1 2004/02/01 21:54:25 jeff Exp $

From 1.99_05 and earlier:

Build and install extproc_perl as usual.  The C wrapper code has changed to
support NULL indicator checking on Oracle data types, so you will need to
recreate the wrappers for each directly executed procedure you have and relink
using "make -f perlxsi.mk".
